package main

// Define constants for topic names
const (
	SYSTEM_AUDIT_EVENT         = "system.audit.event"
	USER_LOGIN_ATTEMPT         = "user.login.attempt"
	USER_LOGIN_ATTEMPT_OUTCOME = "user.login.attempt.outcome"
)
